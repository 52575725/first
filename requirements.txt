# Micro-course Video Dependencies / 微课视频项目依赖
# =============================================
#
# Full dependency list stays under the legacy skill path used by the current runtime.
# 当前运行链路仍复用 legacy skill 路径，因此完整依赖清单保留在对应目录下。
#
# Install / 安装方式：
#   pip install -r requirements.txt                     # from repo root / 仓库根目录
#   pip install -r skills/ppt-master/requirements.txt   # compatibility path / 兼容路径
#
-r skills/ppt-master/requirements.txt
