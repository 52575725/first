# PPT Master 微课视频生成系统 - 使用指南

## 完整工作流程

### 标准流程（分步执行）

```bash
# Step 1: 创建项目
python3 skills/ppt-master/scripts/project_manager.py init my_course --format ppt169

# Step 2: 导入源文档
python3 skills/ppt-master/scripts/project_manager.py import-sources my_course lesson.pptx --move

# Step 3: 生成PPT内容（按 SKILL.md 执行）
# - Strategist 策划
# - Executor 执行  
# - Post-processing 后处理

# Step 4: 内容分析（可选）
python3 skills/ppt-master/scripts/content_analyzer.py my_course

# Step 5: 生成音频
python3 skills/ppt-master/scripts/notes_to_audio.py my_course \
  --voice zh-CN-XiaoxiaoNeural --rate +0%

# Step 6: 生成字幕（可选）
python3 skills/ppt-master/scripts/subtitle_generator.py my_course

# Step 7: 导出视频
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --resolution 1080p --subtitle
```

### 一键流程（推荐）

```bash
# 创建项目并导入
python3 skills/ppt-master/scripts/project_manager.py init my_course --format ppt169
python3 skills/ppt-master/scripts/project_manager.py import-sources my_course lesson.pptx

# 生成PPT（按 SKILL.md 执行）

# 一键生成音频和视频
python3 skills/ppt-master/scripts/svg_to_pptx.py my_course \
  --recorded-narration audio \
  --export-video \
  --video-resolution 1080p
```

## 高级功能

### 1. 使用高质量TTS

```bash
# ElevenLabs（需要API Key）
export ELEVENLABS_API_KEY="your_key"
python3 skills/ppt-master/scripts/notes_to_audio.py my_course \
  --provider elevenlabs \
  --voice-id "your_voice_id" \
  --elevenlabs-model eleven_multilingual_v2

# MiniMax（国内）
export MINIMAX_API_KEY="your_key"
python3 skills/ppt-master/scripts/notes_to_audio.py my_course \
  --provider minimax \
  --voice-id "female-tianmei" \
  --minimax-model speech-2.8-hd
```

### 2. 添加背景音乐

```bash
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --bgm background.mp3 \
  --bgm-volume 0.2  # 音量 0.0-1.0
```

### 3. 自定义视频参数

```bash
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --resolution 4k \
  --fps 60 \
  --codec libx265 \
  --padding 1.0  # 每页停顿时间
```

### 4. 批量处理

```bash
# 批量生成多个课程
for project in projects/*/; do
  echo "Processing $project..."
  python3 skills/ppt-master/scripts/notes_to_audio.py "$project" \
    --voice zh-CN-XiaoxiaoNeural
  python3 skills/ppt-master/scripts/pptx_to_video.py "$project" \
    --resolution 1080p
done
```

## 音色选择指南

### 中文音色推荐

**稳重商务风格**：
- `zh-CN-YunjianNeural` - 稳重男声，适合财报、咨询
- `zh-CN-XiaoxiaoNeural` - 清晰女声，适合教学

**活泼教学风格**：
- `zh-CN-XiaoyiNeural` - 明亮女声，适合产品介绍
- `zh-CN-YunxiNeural` - 年轻男声，适合科技内容

**播报风格**：
- `zh-CN-YunyangNeural` - 播报男声，适合发布会

### 英文音色推荐

- `en-US-GuyNeural` - 稳重男声
- `en-US-JennyNeural` - 清晰女声
- `en-US-AriaNeural` - 自然女声

### 查看所有可用音色

```bash
# edge-tts 音色列表
python3 skills/ppt-master/scripts/notes_to_audio.py --list-voices --locale zh-CN

# 查看推荐音色
python3 skills/ppt-master/scripts/notes_to_audio.py --list-common-voices
```

## 性能优化技巧

### 1. 启用音频缓存

音频缓存自动启用，位于 `~/.ppt-master/audio_cache/`

```bash
# 查看缓存状态
python3 skills/ppt-master/scripts/audio_optimizer.py

# 清理缓存（如果超过500MB）
# 自动清理最旧的文件
```

### 2. 并行处理

音频生成已自动并行化，无需额外配置。

### 3. 增量更新

只重新生成修改过的页面：

```bash
# 修改某个SVG后，只重新生成该页
# 系统会自动检测文件修改时间
python3 skills/ppt-master/scripts/pptx_to_video.py my_course
```

## 故障排查

### 问题1：ffmpeg not found

```bash
# 检查安装
ffmpeg -version

# Ubuntu/Debian
sudo apt install ffmpeg

# macOS
brew install ffmpeg

# Windows
# 下载 https://ffmpeg.org/download.html
# 添加到系统PATH
```

### 问题2：音频生成失败

```bash
# 检查TTS依赖
pip install edge-tts

# 检查API Key（如果使用云服务）
echo $ELEVENLABS_API_KEY
echo $MINIMAX_API_KEY
```

### 问题3：SVG转PNG失败

```bash
# 安装cairosvg
pip install cairosvg

# 或安装Inkscape
# Ubuntu: sudo apt install inkscape
# macOS: brew install inkscape
```

### 问题4：视频文件太大

```bash
# 使用H.265编码（更高压缩率）
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --codec libx265

# 或降低分辨率
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --resolution 720p
```

## 输出文件说明

```
my_course/
├── audio/                    # 音频文件
│   ├── 01_cover.mp3
│   ├── 02_intro.mp3
│   └── ...
├── exports/                  # 导出文件
│   ├── my_course.mp4         # 最终视频
│   ├── my_course.srt         # 字幕文件
│   └── my_course.pptx        # 带音频的PPTX
├── temp_video/               # 临时文件（可删除）
│   ├── *.png                 # PNG图片
│   └── segment_*.mp4         # 视频片段
└── content_analysis.md       # 内容分析报告
```

## 最佳实践

### 1. 音频质量

- **默认场景**：使用 edge-tts（免费、稳定）
- **高质量需求**：使用 ElevenLabs 或 MiniMax
- **音色克隆**：提供样本音频给 ElevenLabs

### 2. 视频质量

- **在线观看**：1080p + H.264
- **高清存档**：4K + H.265
- **快速预览**：720p + H.264

### 3. 内容优化

- 运行 `content_analyzer.py` 检查内容密度
- 图表密集页面增加停顿时间
- 使用字幕提高可访问性

### 4. 工作流程

1. 先生成低分辨率预览（720p）
2. 确认效果后生成高清版本（1080p/4K）
3. 保留源文件便于后续修改

---

**版本**：v1.0  
**更新**：2026-05-11
