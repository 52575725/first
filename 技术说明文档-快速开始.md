# PPT Master 微课视频生成系统 - 快速开始

## 系统概述

PPT Master 微课视频生成系统能够将PPT/教案自动转换为带讲解旁白的高质量微课视频。

### 核心功能

✅ **智能内容解析** - 自动提取知识点结构、教学节奏  
✅ **AI语音合成** - 支持多个TTS提供商，自然流畅的讲解  
✅ **自动视频编辑** - 画面与语音精确对齐  
✅ **一键视频导出** - 生成标准MP4格式微课视频  
✅ **字幕生成** - 自动生成SRT字幕文件  

## 快速开始

### 1. 环境准备

```bash
# 安装Python依赖
pip install edge-tts Pillow cairosvg

# 安装系统依赖（必需）
# Ubuntu/Debian
sudo apt install ffmpeg

# macOS
brew install ffmpeg

# Windows - 下载安装 https://ffmpeg.org/download.html
```

### 2. 创建项目

```bash
# 创建新项目
python3 skills/ppt-master/scripts/project_manager.py init my_course --format ppt169

# 导入源文档
python3 skills/ppt-master/scripts/project_manager.py import-sources my_course lesson.pptx
```

### 3. 生成PPT内容

按照 `skills/ppt-master/SKILL.md` 执行完整的PPT生成流程：
- Strategist 策划
- Executor 执行
- Post-processing 后处理

### 4. 生成音频旁白

```bash
# 使用默认TTS（edge-tts）
python3 skills/ppt-master/scripts/notes_to_audio.py my_course \
  --voice zh-CN-XiaoxiaoNeural --rate +0%
```

### 5. 导出视频

```bash
# 基础导出（1080p）
python3 skills/ppt-master/scripts/pptx_to_video.py my_course

# 高级导出（带字幕和背景音乐）
python3 skills/ppt-master/scripts/pptx_to_video.py my_course \
  --resolution 1080p \
  --subtitle \
  --bgm background.mp3 \
  --bgm-volume 0.2
```

## 一键生成（推荐）

```bash
# 自动完成音频生成 + 视频导出
python3 skills/ppt-master/scripts/svg_to_pptx.py my_course \
  --recorded-narration audio \
  --export-video \
  --video-resolution 1080p
```

## 输出文件

生成的文件位于项目目录：

```
my_course/
├── audio/              # 音频文件（MP3）
├── exports/            # 导出的视频和PPTX
│   ├── my_course.mp4   # 最终视频
│   └── my_course.srt   # 字幕文件（如果启用）
└── content_analysis.md # 内容分析报告
```

## 常见问题

**Q: 视频生成失败？**  
A: 检查 ffmpeg 是否正确安装：`ffmpeg -version`

**Q: 音频质量不满意？**  
A: 尝试使用高质量TTS提供商（ElevenLabs/MiniMax），需要API Key

**Q: 视频文件太大？**  
A: 使用 H.265 编码器：`--codec libx265`

## 下一步

- 查看 `技术说明文档-架构设计.md` 了解系统架构
- 查看 `技术说明文档-使用指南.md` 了解详细功能
- 查看 `skills/ppt-master/SKILL.md` 了解完整PPT生成流程
